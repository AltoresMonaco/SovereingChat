services:
  api:
    ports:
      - "3080:3080"
    depends_on: null          # supprime totalement la dépendance du fichier de base
    user: "1000:1000"
    environment:
      - HOST=0.0.0.0
      - MONGO_URI=${MONGO_URI}
      - MEILI_HOST=${MEILI_HOST}
      - MEILI_NO_ANALYTICS=${MEILI_NO_ANALYTICS}
      - RAG_ENABLED=${RAG_ENABLED}
      - RAG_API_URL=${RAG_API_URL}
      - RAG_PORT=${RAG_PORT:-8000}
      - CREDS_KEY=${CREDS_KEY}
      - CREDS_IV=${CREDS_IV}
      - SEARCH=${SEARCH}
      - ALLOW_REGISTRATION=${ALLOW_REGISTRATION}
      - ALLOW_EMAIL_LOGIN=${ALLOW_EMAIL_LOGIN}
      - ALLOW_SOCIAL_LOGIN=${ALLOW_SOCIAL_LOGIN}
    volumes:
      - /srv/librechat-data/uploads:/app/client/public/uploads

  meilisearch: { profiles: ["disabled"] }
  mongodb:     { profiles: ["disabled"] }
  rag_api:     { profiles: ["disabled"] }
  vectordb:    { profiles: ["disabled"] }
